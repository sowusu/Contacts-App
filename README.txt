Setup:

1. Run create table queries in contacts.sql file in order
2. Run import_data.php script to preload csv data from Contacts.csv (make sure csv file is same directory as php script)
3. Initiating the app using index.php